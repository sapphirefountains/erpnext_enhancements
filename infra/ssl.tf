# SSL is managed via google_compute_managed_ssl_certificate resources
# created by the net-lb-app-ext module (ssl_certificates.managed_configs).
# Domain verification CNAME records must be added manually at the DNS provider.
# No Certificate Manager resources are used.
