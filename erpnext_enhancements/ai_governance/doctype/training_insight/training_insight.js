/**
 * Desk form script for the Training Insight doctype.
 *
 * Loaded automatically when the Training Insight form opens. Currently a
 * placeholder: the `refresh` handler is an empty stub reserved for future
 * client behaviour.
 */
frappe.ui.form.on('Training Insight', {
	refresh: function(frm) {

	}
});
