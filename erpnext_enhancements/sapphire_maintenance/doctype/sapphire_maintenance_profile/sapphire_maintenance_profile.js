/**
 * Desk form script for the Sapphire Maintenance Profile doctype.
 *
 * Loaded automatically by Frappe when the Maintenance Profile form opens.
 * Currently a placeholder: the `refresh` handler is a stub reserved for future
 * form indicators. The profile's data (safety instructions, access codes) is
 * consumed elsewhere — by the Maintenance Record dashboard widget — not here.
 */
frappe.ui.form.on("Sapphire Maintenance Profile", {
	refresh: function(frm) {
		// Add indicators if needed
	}
});
