# `api/` — Whitelisted HTTP endpoints

This package holds the app's Frappe **whitelisted endpoints** (`@frappe.whitelist()` functions reachable over HTTP), plus a handful of doc-event hooks, background workers, and scheduler jobs that live alongside them. Endpoints are called from the app's desk client scripts (`public/js/...`), the standalone Time Kiosk PWA, desk Pages, and external server-to-server webhooks (Twilio / the "Triton" telephony gateway).

Every function is documented inline. This README is the map.

> ⚠️ **Mixed indentation — match the file you are editing.** The package is split roughly in half, not "mostly spaces" as this note used to say. **Tab-indented** (measured v1.521.0 by counting leading tabs in every file): `analytics`, `briefing`, `call_recording_export`, `collab`, `comments`, `dashboard_widgets`, `design_dashboard`, `desk_shortcuts`, `device_dashboard`, `device_management`, `executive_dashboard`, `finance_calendar`, `finance_dashboard`, `gantt`, `horoscope`, `hr_dashboard`, `integrations_health`, `inventory_scanner`, `kpi`, `marketing_dashboard`, `operations_dashboard`, `pickup_routing`, `production_dashboard`, `sales_dashboard`, `stock_scan`, `task_dashboard`, `travel`, `user_drafts`. Every other file is 4 spaces. A new file takes tabs, the `.editorconfig` default.

## File map

| File | Purpose | Key whitelisted functions | Called by | External services |
|---|---|---|---|---|
| `activity_log.py` | Timeline badge counts for a document | `get_activity_counts` | `public/js/activity_log_numbering.js` | — |
| `analytics.py` | GA4 + Search Console dashboard data; the Search Console snapshot backfill (bench-only, not whitelisted) | `get_ga4_data`, `get_gsc_data`; `backfill_gsc_snapshots` via `bench execute` | `enhancements_core/page/ga4_dashboard/ga4_dashboard.js` | Google Analytics 4 Data API, Google Search Console API |
| `booking.py` | Composite (Travel/Rental/Maintenance) asset booking, **and** the rental pre-shipping / return checklist generator. `generate_inspection` returns the existing non-cancelled inspection for a (booking, direction) rather than making a second one, so two people clicking at once cannot produce two sheets for one crate. Rows come from the submitted pre-shipping sheet for a Return — `qty_expected` set to what was actually counted *out*, since "are all items accounted for" is a question about what left the yard — and from `resolve_checklist_template` otherwise (Asset template, then Asset Category). It **throws** when it has neither: an empty checklist submits clean and reads as a complete return, which is the worst output this feature could produce | `create_composite_booking`, `generate_inspection` | `asset_booking.js` (Rental bookings only — composite bookings wrap every rental in a Travel and a Maintenance leg) | — |
| `briefing.py` | Per-user Morning Briefing: weekday 06:30 cron pre-generates + caches one briefing per recipient (Daily Briefing doctype) — tasks/calendar/pipeline/ToDos narrated by Gemini with a deterministic markdown fallback; optional per-recipient email | `get_morning_briefing` (`force=1` regenerates); cron `scheduled_briefing_run` → bg `generate_briefings_for_all_users`; daily `purge_old_briefings` | "Morning Briefing" Custom HTML Block | Gemini Enterprise Agent Platform (via `gemini.py`) |
| `call_intelligence.py` | Upserts AI post-call analysis onto the stock **Call Log** (docname == Twilio SID): sentiment, escalation risk, follow-ups, topics, compliance flags, CSAT, IVR intent, agent; idempotent, partial updates never blank fields | `process_call_intelligence`; helper `upsert_call_log` (called by `telephony.process_unified_recording`) | external Triton gateway webhook | — |
| `collab.py` | Live collaborative editing relay — validates and re-publishes field changes + per-field focus presence to the document's realtime room; never writes to the DB | `broadcast_field_update`, `broadcast_focus` | `public/js/collab/live_form_sync.js` | — |
| `comments.py` | Custom comment CRUD + file linking (backs the Vue Comments App) | `get_comments`, `add_comment`, `update_comment`, `delete_comment`, `link_files_to_comment` | `comments.js`, `global_comments.js`, `crm_note_enhancements.js` | — |
| `communication.py` | AI email/SMS reply drafting | `suggest_sms_reply`; hook `after_insert_communication`; worker `generate_draft_response` | `communication.js`; `Communication` `after_insert` hook | Gemini Enterprise Agent Platform (via `gemini.py`) |
| `dashboard_widgets.py` | Shared gating for the department dashboard widgets: `WIDGETS` maps each widget to its `ERPNext Enhancements Settings` Check (all default **OFF**), and the `widget_feed` decorator applies the department role gate + toggle so a disabled widget returns `{"enabled": false}` instead of an error. Roles are **imported from `kpi.py`**, never re-declared, so a widget can never be visible to someone who cannot see the same department's KPI Cockpit. Feeds take no arguments by design — nothing to validate, and no way for a caller to widen a query | — (helpers: `can_view`, `require_department`, `widget_enabled`, `widget_feed`, `fetch_all`) | every `*_dashboard.py` feed module below | — |
| `design_dashboard.py` | Design Dashboard widgets over `Water Feature Design`: in-flight WIP by status, the Reviewed-but-not-Issued sign-off queue, designs carrying blockers/warnings before hand-off, and hydraulic headroom (delivered `design_flow_gpm` against `required_circulation_gpm`). Designs with no circulation requirement are skipped rather than shown as infinite margin — a missing denominator is a data gap, not good news | `get_wip_board`, `get_signoff_queue`, `get_handoff_readiness`, `get_hydraulic_headroom` | the four Design Dashboard Custom HTML Blocks | — |
| `feedback.py` | The `/feedback` SPA's whole surface, plus the capture widget's `submit_capture` (WI-079 slice 2: System Users only, snapshot stored as a private JSON File) — employee bug/feature intake, System-Manager triage, and the confirm step that turns a reviewed proposal into `Task`s on PRJ-00580 / PRJ-00755. **Nothing here creates a `Task`**: `create_tasks` delegates to `product_feedback/task_writer.py`, which is the only module allowed to, and `tests/test_feedback_endpoint_surface.py` asserts that structurally. Identity is derived, never accepted — `requested_by`, `status`, `decided_by` and `created_task` are dropped from any payload, and refused fields are *reported back* in `rejected` rather than lost silently (the `training_author.save_draft_version` rule). Attachments are linked server-side after an ownership check, because the requester deliberately holds no write permission on their own request: write would let them move `status`, and `Submitted → Approved` is a legal transition | `get_bootstrap`, `get_all_requests`, `get_request`, `submit_request`, `submit_capture`, `draft_description`, `review_decision`, `rerun_breakdown`, `save_proposal`, `create_tasks` | `public/js/feedback/transport.js` (the `M` map; `submit_capture` is dialled by the capture panel, which reuses the transport) | Triton `POST /api/v1/planning/work-breakdown`, via `product_feedback/triton_client.py` in a background job |
| `executive_dashboard.py` | Executive Dashboard widgets. **Snapshot-only** — every figure comes from the nightly `KPI Snapshot`, because a cross-department view exists to be compared across departments and across days, and a number that changes on every refresh can do neither. The Risk Queue therefore reports snapshot staleness and failed runs *above* Bad KPIs: a department whose run failed has no Bad KPIs at all. Drill-through links render only for departments the viewer can open | `get_scorecard`, `get_cash_runway`, `get_bookings_backlog`, `get_risk_queue` | the four Executive Dashboard Custom HTML Blocks | — |
| `gantt.py` | Data feed **and drag-to-edit writes** for the embeddable Gantt widget: validates the client-supplied config (doctype, field map, filters, dependency child table) against `frappe.get_meta`, then returns permission-checked rows shaped for DHTMLX (`tasks` + `links`). Composite mode (`group_by` + `children`) nests a second doctype's rows under each root with prefixed ids and `ref_doctype`/`ref_name` per row — powers the Projects Dashboard portfolio Gantt (Master Project **or** project type → Project → Tasks). `group_by` accepts a list (first non-empty value wins); `extra_fields` passes validated raw column values through for client-side colouring; `children.lazy` returns only a per-root child **count** so the client can render a caret and fetch one subtree on expand. `export_gantt_data` returns the same rows as a base64 CSV/XLSX payload — it delegates to `get_gantt_data`, so it inherits every permission gate and validator rather than re-implementing them, and it forces `children.lazy = False` for the export only (on screen lazy branches are the point; in a file, exporting just the roots somebody expanded silently omits most of the schedule). It also takes an optional inclusive `from_date`/`to_date` pair and narrows the file to the tasks that touch that window, keeping the ancestors of the ones that do (the Level column is the only hierarchy a spreadsheet has). The window is applied to the **shaped** rows, not pushed into the query as filters: the dates a task is plotted at are not always the dates its columns hold — a row with only an end gets a one-day bar back-dated from it, a date-only end is pushed forward a day — so a SQL predicate on the raw columns would disagree with the chart the file is meant to match. Supplying only one bound throws rather than guessing the other | `get_gantt_data`, `update_gantt_row`, `export_gantt_data` | `public/js/gantt_widget/gantt_widget.js` (the Project Schedule tab; the "Projects Dashboard" Custom HTML Block) | — |
| `gemini.py` | Gemini Enterprise Agent Platform (formerly Vertex AI) REST client, internal helper: one `generateContent` call per draft on the **global** endpoint, OAuth2 bearer minted from the Drive service account, token accounting to AI Model Usage. `build_endpoint` is a function so the URL shape is testable — the regional literal it replaced 404'd the briefing every weekday for a week | `generate_content_with_vertex_ai` (name kept: five callers), `build_endpoint` | imported by `briefing.py`, `communication.py`, `training_ai.py`, `assistant_tools/draft_course_spec.py` | Gemini Enterprise Agent Platform `generateContent` (`gemini-3.8-flash`) |
| `hr_dashboard.py` | HR Dashboard widgets: overdue/due-soon `Training Assignment`s, time-capture gaps, headcount movement, and work anniversaries. `hrms` is not installed here, so everything reads `Employee` plus this app's Training and Job Interval doctypes. Time capture stays silent when **no** source captured any time in the window rather than listing every employee as delinquent | `get_training_compliance`, `get_timesheet_completeness`, `get_headcount_movement`, `get_people_calendar` | the four HR Dashboard Custom HTML Blocks | — |
| `integrations_health.py` | System-Manager-only health snapshot of every external integration (QuickBooks token/CDC/failed-syncs, Drive configured?/sync-log failures, Triton/Twilio, Gemini, GA4/GSC) + scheduler liveness + 24 h Error Log digest; secrets read only as "configured?" booleans. DB-only on load; the one live check (`run_drive_test`) is opt-in | `get_health`, `run_drive_test` | `enhancements_core/page/integrations_health/integrations_health.js` | — (live check proxies `crm_enhancements.drive_sync.test_connection` → Google Drive API) |
| `inventory_scanner.py` | The Inventory Scanner Audit desk page — physical counts. Scan a location, scan items, type what is there; lines accumulate in a resumable `Inventory Count Session` with the system quantity snapshotted at scan time, and finalize sums them per (item, warehouse) into a **draft** Stock Reconciliation for a Stock Manager to submit, so nothing here touches the stock ledger. The variance-reason rule is enforced at finalize, where an item's count across bins is complete, not per scan. `resolve_scan` reads a Storage Location barcode or name, then — since v1.521.0 — a Stock Scan warehouse QR label or a bare warehouse name, then an item; the warehouse check sits *after* Storage Location so an existing barcode never changes meaning, and a group or disabled warehouse comes back `unknown` with a `message` rather than a throw. Gated to `ALLOWED_ROLES` (System Manager, Stock Manager, Inventory Clerk); session writes use `ignore_permissions` after the gate | `get_bootstrap`, `resolve_scan`, `lookup_item`, `get_active_session`, `start_session`, `add_count`, `remove_line`, `cancel_session`, `finalize_session` | `inventory_enhancements/page/inventory_scanner_audit` | — |
| `logger.py` | Client-side error reporting sink | `log_client_error` | browser JS | — |
| `maintenance_scheduling.py` | Predictive next-visit dating: rolls Sapphire Contract Feature dates forward and mirrors them to Sales Order Items | `update_next_visit_dates` (on_submit hook), `calculate_next_date` | `Sapphire Maintenance Record` `on_submit` hook | — |
| `maintenance_visit.py` | Visit Wizard backend: bootstrap (load + server-side template instantiation), autosave with field allowlist + optimistic locking, workflow-aware finish, forward-looking "pull a future visit forward" list + create, and the unscheduled/backfill path — session permissions throughout | `get_visit_bootstrap` (returns `features`: Serial No -> item_name for the tab labels), `save_visit`, `finish_visit`, `get_upcoming_visits` (un-drafted features due in 8–30 days), `create_visit_today` (extra one-off, `EXTRA_VISIT_LABEL`), `get_loggable_sites` (every Active contract, **no** date window), `create_visit` (any site, today or backdated to `MAX_BACKFILL_DAYS`, deliberately **unlabelled**) | `sapphire_maintenance/page/visit_wizard` | — |
| `maintenance_workflow.py` | Post-submit automation (stock / timesheet / warranty claim / invoice / reading log) | `process_maintenance_submission` (bg worker) + step helpers, `resolve_consumable_warehouse`, `build_stock_entry_rows` | enqueued from the Sapphire Maintenance Record controller | — |
| `pickup_routing.py` | Supplier pick-up run for a Project's Purchase Orders: the POs still to collect, collapsed into one stop per supplier pick-up address, plus the depot and the Google Maps browser key. Resolves each stop through a four-step address chain (`po.dispatch_address` → `po.supplier_address` → `Supplier.supplier_primary_address` → the Address directory) and reports which step won in `address_source`; `po.shipping_address` is excluded on purpose (our own yard). The travelling-salesman ordering happens client-side in `DirectionsService`, so this module calls no Google API of its own. A stop also carries `latitude`/`longitude` (else `null`) when its Address was picked from the Places autocomplete, letting the client skip a billable geocode and route to the exact building. `get_supplier_pick_data` (v1.338.0) turns the same run inside out — one or more *suppliers*, every job — and returns the identical payload shape with `project: null` and each line carrying its own job, so one client implementation serves both | `get_pickup_route_data`, `get_supplier_pick_data` | `public/js/project_enhancements/pick_routing_map.js` (Project Budget tab; Supplier form button and Supplier list action via `public/js/procurement/supplier_pick_sheet.js` and `public/js/global_enhancements/supplier_list.js`) | — (returns the Travel Settings Google Maps browser key for the client to use) |
| `procurement.py` | Supplier purchase-link store; `receive_items`, the confirm behind the Purchase Order's *Receive Items* dialog (ER-2026-458194) — builds a Purchase Receipt for the quantities typed with ERPNext's own `make_purchase_receipt` mapper, refuses over-receipt in the buyer's terms using the allowance ERPNext would apply (`procurement_quantities.plan_receipt`, pure), and inserts and submits it through the framework's own permission checks, so `received_qty`, `per_received`, the status pill and (via `po_order_stage`) the Order Stage all move on their own. Since v1.521.0 it also takes an optional `warehouse` — receive into that location instead of the order line's, set on the header `set_warehouse` **and** every row (ERPNext never pushes the header into rows server-side, it only clears a header that disagrees), refused in the receiver's terms first if it is a group, disabled or another company's (`_receiving_warehouse`) — and `remarks`. It grants nothing new: whoever may create and submit a receipt can already change its warehouse on Create > Purchase Receipt. Beside it, the **non-whitelisted** `receive_order_line`, the Stock Scan page's "+": it converts the page's stock-UOM count into the order line's UOM (`stock_scan_rules.to_order_uom`, which refuses a result that is not whole in a whole-number UOM rather than rounding it) and delegates to `receive_items`, owning no check of its own, so the page and the dialog cannot disagree about what may be received; plus the Purchase Order `before_validate` handler that pushes the header's expected delivery date into item rows that have none. That handler is **not** the Required By cascade and must not become one: ERPNext already cascades `schedule_date` in `buying_controller.validate_schedule_date()` and does more than cascade it (the header is pulled up to the earliest row). Fills blanks only, never rolls up — a per-item override is the point of ER-2026-362239 | `get_item_links`, `save_item_link`, `receive_items`; `receive_order_line` (not whitelisted); hook `cascade_expected_delivery_date` | `procurement_links.js`, `po_receive_items.js`; `api/stock_scan.add`; Purchase Order `doc_events` | — |
| `search.py` | AwesomeBar global-search augmentation | `search_global_docs` | `erpnext_enhancements.js` | — |
| `stock_scan.py` | The `/stock-scan` page's whole surface (v1.521.0): what a scanned label or typed code is, a location's items, an item's card (on hand here and elsewhere, its open order lines), three searches, and the four saves — **Take** (a submitted Material Issue, optionally charged to a job), **Add** against an open order line (a Purchase Receipt into the scanned location, through `procurement.receive_order_line`) or explicitly without one (a Material Receipt at the item's current cost, the log flagged `needs_review`) — with neither it refuses, because stock that is on an order received without it double-counts when the order's own receipt arrives — and **Move here** (a Material Transfer, for put-away); plus **Undo** and the caller's recent saves. Every save inserts its `Stock Scan Log` row **before** the voucher, keyed on a unique client-minted `client_ref`, so a double tap or a retry after a dropped connection returns the first save (`repeated: true`) instead of posting twice. Every Stock Entry sets `stock_entry_type` (mandatory on v16; `purpose` is only fetched from it) and a difference account on each row from `inventory_enhancements/stock_accounts.py`, because production's Company has no Stock Adjustment Account and ERPNext's own chain comes up empty. `resolve` never throws — an unknown or unusable code comes back as `{kind: "unknown", message}`, since a caught `frappe.throw` still shows its modal; `get_location` and `get_item` do throw on a bad location or an unknown item, and the page shows that inline. An add without a PO **with** a job is a return from that job and offsets to the Parts Taken account, so the job's cost nets by project; without a job it offsets to the Added Without PO account. `search_projects` lists with `get_all` behind a doctype-level read check, because this site's per-Employee User Permissions would otherwise hide every job owned or led by someone else. Undo refuses a voucher that was canceled **and amended** in the Desk (the amendment still moves stock). Every judgement is in `inventory_enhancements/stock_scan_rules.py`, pure and tested bench-free. `boot_payload` (not whitelisted) feeds `www/stock_scan.py` | `resolve`, `get_location`, `get_item`, `search_items`, `search_locations`, `search_projects`, `take`, `add`, `move_here`, `undo`, `get_recent` | `public/js/stock_scan/transport.js` (the `M` map); `www/stock_scan.py` | — |
| `task_dashboard.py` | Morning TV screen data: top-10 ranked projects (PM/tech lead), overdue + today's tasks with assignee names, today's public events; plus the Wall Display payload (per-project task-completion stats, wall settings, deploy version) | `get_task_dashboard_data`, `get_wall_dashboard_data` | "Task Dashboard" Custom HTML Block (`Custom HTML Block/task_dashboard.js`); `/wall` display (`public/js/wall/app.js`) | — |
| `telephony.py` | Triton/Twilio voice + SMS integration, and the inbound call-routing contract the gateway reads | many (see below) | external Triton/Twilio webhooks; `contact.js`/`customer.js`/`lead.js`/`telephony_client.js`; `call_routing_settings.js` | Twilio (signatures, Voice JWT), Triton gateway HTTP API |
| `maintenance_board.py` | Maintenance Day Board feed (scheduled / clocked-in / submitted today / flagged), role-gated | `get_day_board_data` | `sapphire_maintenance/page/maintenance_day_board` | — |
| `marketing_dashboard.py` | Marketing Dashboard widgets: the session→lead→opportunity→won cascade, month-to-date spend per channel with cost per lead, unsourced leads as a fixable worklist, and GA4/Search Console pull health. Attribution reads **`custom_lead_source`, never `source`** (the column erpnext v15 orphaned; see `kpi_dashboards/README.md`), and the `Unknown (pre-Aug 2026)` bucket counts as unsourced. A failed GA4 pull renders blank, not `0` — zero sessions is a different claim | `get_funnel_cascade`, `get_channel_spend`, `get_unsourced_leads`, `get_source_health` | the four Marketing Dashboard Custom HTML Blocks | — |
| `operations_dashboard.py` | Operations Dashboard widget: device fleet compliance. `Managed Device` is an optional doctype and is guarded, so a site without it sees "not installed" rather than a broken block. The three maintenance feeds moved to `service_dashboard.py` in v1.530.0 | `get_fleet_health` | the Operations Fleet Health Custom HTML Block | — |
| `service_dashboard.py` | Service Dashboard widgets (v1.530.0, moved from Operations): today's visit board, out-of-range chemistry alerts, and the labour-capture gap (visits that reached the terminal workflow state with neither a clock-in nor a labour cost). Gated on the Service roles in `kpi.py`; the toggles keep their `operations_*` fieldnames | `get_day_board`, `get_chemistry_alerts`, `get_labor_capture` | the three Service Dashboard Custom HTML Blocks | — |
| `production_dashboard.py` | Production Dashboard widgets: build WIP and aging, overdue `Project Process Step`s, material requests not yet received, and hours against budget. The hours feed computes in Python because the two Project fields disagree about units — `custom_total_time_elapsed` is a **Duration (seconds)** and `custom_time_budget_in_hours` is a **Data** field of hours — so actual is divided by 3600 first and an unparseable budget is skipped, not ranked as an infinite overrun | `get_wip_aging`, `get_milestone_slippage`, `get_material_readiness`, `get_hours_variance` | the four Production Dashboard Custom HTML Blocks | — |
| `quality_inspection.py` | Generates a **frozen** Quality inspection from a milestone: resolves the Active master template and the project's locked Scope of Work, merges them, hashes the result and writes a draft. Refuses a second open inspection for the milestone, a non-Active template, an empty inspection, and a merge with duplicate provenance — and reports contracted criteria that name no milestone rather than dropping them silently. Merge/hash logic is frappe-free in `quality/merge.py` so the freeze is testable | `generate_inspection` | Quality Control desk (WI-075 sub-phase D) | — |
| `quality_ncr.py` | Acknowledging a Critical non-conformance alert, and reading who is still silent. Always stamps `frappe.session.user` — there is no recipient parameter, because an endpoint that accepted one would let a single caller tick all three rows. Refuses anybody not on the notified list: an acknowledgement from a person nobody told is a green tick with nothing behind it. Gated on `read`, not `write` — a President who can open the NCR should be able to say they have seen it without holding write access to the quality record | `acknowledge_critical_alert`, `critical_alert_state` | `public/js/quality/non_conformance.js` (WI-075 sub-phase G) | — |
| `change_order.py` | Recording approval on a `Change Order`. The stamps are **read-only on the form and written only here**, from the server clock and the session user — the old client path on `complete_step` let the browser propose its own timestamp and the audit found retroactive box-ticking. Two approvals, two different acts: the PM approves that the work and the figure are right, the customer approves that they will pay. `how` is **required** on the customer's, because an approval nobody can point at is not a record. Totals count only locked change orders — a proposal in the contract value is how a number nobody agreed to reaches a dashboard | `approve_change_order`, `record_customer_approval`, `revoke_approval`, `get_project_change_orders` | Change Order form (WI-075 sub-phase K) | — |
| `project_budget.py` | A project's budget by category, and the approvals a reallocation needs. Every approval is stamped from the **server session and the server clock**, never from a value the browser proposed. `give_second_approval` carries the control: it refuses a session user who is the requester or the project manager who already approved, because a protected-category rule satisfiable by one person clicking twice is a control in appearance only. `get_project_budget` returns `variance_percent` as **null** on a zero-budget line rather than 0 ("on budget") or a huge number, and reports `unclassified` purchase spend beside the categories rather than inside them — folding it in would invent an attribution nobody made | `approve_reallocation`, `give_second_approval`, `revoke_approval`, `get_project_budget`, `get_project_reallocations` | Project budget section, Budget Reallocation form (WI-075 sub-phase M) | — |
| `subcontractor_scorecard.py` | Reading and adjusting a subcontractor scorecard. **No endpoint returns a bare score** — `state` and `score_display` travel with every numeric field, because a scorecard's number quoted without how much of it is real is the failure this sub-phase exists to prevent. `get_period_summary` surfaces `not_measurable` as a first-class figure rather than leaving it to be counted from the rows; today it equals the total. `record_adjustment` refuses a non-zero adjustment with no reason and stamps the approver from the session and the server clock; `build_scorecard` refuses a month that has not finished | `get_scorecard`, `get_supplier_scorecards`, `build_scorecard`, `record_adjustment`, `get_measure_catalog`, `get_period_summary` | Subcontractor Scorecard form, Supplier form (WI-075 sub-phase N) | — |
| `quality_due.py` | Which inspection milestones have come round on a project, and what is in the way. Read-only and recomputed from current state — nothing is stored and nothing is generated. Returns **every** milestone rather than only the due ones, because a due-only list loses "why has that one not come round" and "why is that one not showing". Two of its states are findings: `blocked` (due, and nobody has written the checklist) and `unknown` (the build status is not a recognised option, so due-ness is not knowable — reporting that as "not due" is what would make the sweep read clean forever) | `get_project_milestones`, `get_my_due_inspections` | `public/js/quality/project_inspections.js` (WI-075 sub-phase I) | — |
| `quality_wizard.py` | Read and write side of the field inspection wizard: one bootstrap call, an autosaving field-allowlisted patch with optimistic locking, and a submit. Modelled on `maintenance_visit.py`. **The row allowlist is the freeze's last line of defence** — only `outcome`, `measured_value`, `notes` and `photo` are writable, because a wizard that could write `min_value` could turn a failing measurement into a passing one from a phone; there is no append path, since every row was frozen at generation. A stale write is refused, not merged | `get_inspection_bootstrap`, `save_inspection`, `finish_inspection`, `get_open_inspections` | `quality/page/inspection_wizard` (WI-075 sub-phase H) | — |
| `sales_dashboard.py` | Sales Dashboard widgets: speed-to-lead (Leads with no **Sent** `Communication` yet), stalled deals, the Closed-Won→Project hand-off backlog, and contracts inside the renewal horizon. Where a KPI already counts the same thing the widget reuses its threshold — a widget that disagrees with the number above it is worse than no widget | `get_speed_to_lead`, `get_stalled_deals`, `get_handoff_backlog`, `get_renewal_radar` | the four Sales Dashboard Custom HTML Blocks | — |
| `time_kiosk.py` | Time tracking + geolocation | `log_time` (Start/Pause/Resume/Switch/Stop; anchor fix, site + off-site verdict, position + pay rate stamped at Start, tracking health at close), `get_current_status`, `get_projects`, `get_kiosk_options` (projects with site lat/lng, recent projects, geofence radius), `get_tasks_for_project`, `get_maintenance_context` (maintenance-form link + submitted-since check for the active job), `get_my_visits_today`, `get_nearby_visit` (geofenced clock-in suggestion), `get_my_day` / `get_my_history` / `get_my_trail` / `get_shift_summary` (the technician's own views), `submit_correction_request` / `get_my_correction_requests` / `cancel_correction_request` (review is `workforce/corrections.py`), `link_attachment`, `record_job_photo` / `get_pending_photo_uploads` (WP-2 job photos: a row is registered the instant the shutter fires, keyed on a device-minted `client_uid` so a retried offline upload updates instead of duplicating), `log_geolocation`, `log_geolocation_batch` (stores over-threshold fixes as `Low Accuracy`, keeps `fix_count`/`last_fix_at` live), `stash_location_fix` (the browser hands its own geolocation fix straight to the server so the Triton clock-in path can read it back **server-side** -- a lat/lng passed as an MCP tool argument is a number the model typed, and a geofence anchor must never be one; 120 s TTL, keyed by session user, consumed by the clock-in that uses it, and its reader is deliberately NOT whitelisted), `clock_in_with_stashed_fix` (clocks the CALLER in against that fix; no `employee` parameter exists, because on-behalf is clock-out only), `add_manual_interval` (time entered by hand: with an `end_time` it is a finished block on any unlocked day, without one it is an open session that began earlier **today** -- an interval with no end runs forever, so an open backdated start collides with everything logged after it, which is why the two shapes have different rules. Naming another `employee` is allowed for the **block** shape only -- a finished block for someone else is a historical record, an Open one claims they are on the clock right now, and that is the on-behalf clock-in the geofence exists to prevent; never anchored, always stamped `manual_start` with a reason), `start_backdated` (a thin delegate: the caller's own open session, today, and no `employee` parameter for the same reason `clock_in_with_stashed_fix` has none), `update_interval_times` (moves start and/or end and can correct `time_category` -- the three fields in `ALLOWED_UPDATE_FIELDS`, and nothing else, because `read_only` on Job Interval is a Desk hint rather than a server gate; a blank category is ignored rather than written, keeps `original_start_time`/`original_end_time` via the same `corrections._record_originals` the reviewer path uses, and raises `manual_start` only when the start actually moved), `close_interval_for_employee` (self-service or, for `TIMELINE_MANAGER_ROLES`, on behalf of another employee; always an unanchored close, uses the non-throwing `photo_gate.resolve` and records the skip reason verbatim), `get_kiosk_bootstrap`, `get_employees_for_timeline`, `get_live_positions`, `get_location_history` (site, anchors, stats, gaps, stops, day totals), `export_location_history` (csv / gpx), `refresh_tracking_health`; daily `purge_old_location_logs` | `public/js/kiosk/*.js`, `www/kiosk-sw.js`, `www/kiosk.py`, `location_timeline.js`, `job_interval.js` | Google Geocoding (via `workforce/sites.py`, `requests`) |
| `travel.py` | Travel read-side: desk calendar events, `/itinerary` page data, trip-form map (Google Maps key + POIs), itinerary email trigger. A POI's point resolves down three rungs — its own Geolocation, then the linked Address's stored autocomplete point, then the address text for the client to geocode. `get_maps_api_key` is the shared **desk** browser maps key (`Travel Settings.google_maps_api_key`) — any logged-in user may read it, and it has grown non-travel consumers. Since v1.483.2 there is a **second** key beside it, `google_geocoding_api_key`: the browser key is restricted by HTTP referrer, and Google refuses a referrer-restricted key on any *server-side* call, so `workforce/sites.py`'s geocoder needs its own IP-restricted one. Nothing else in Python calls a Google web service — `test_geocoding_key` fails the build if that stops being true | `get_events`, `get_itinerary_bootstrap`, `get_my_trips`, `get_trip_itinerary` (+ reusable `shape_itinerary`), `get_trip_map_data`, `get_maps_api_key`, `get_maps_config` (the same browser key plus the two cloud **Map IDs** -- light and dark -- that `AdvancedMarkerElement` and vector tiles need; blank Map IDs are a working state and mean the caller falls back to a legacy `styles` array), `cache_poi_geocode`, `send_itinerary_email` | `public/js/travel_trip_calendar.js`, `www/itinerary.py`, `public/js/travel/itinerary.js`, `public/js/travel/travel_trip_map.js`, `public/js/travel_trip.js`, `travel_management/doctype/travel_poi/travel_poi.js`, `public/js/global_enhancements/address_autocomplete.js` | — |
| `training.py` | Training **learner runtime** — every call the player makes. One-shot `/training` bootstrap (identity, assigned courses with due dates, the optional library, resume state), per-lesson payloads (never the whole course), watch heartbeats, in-video checkpoints, quizzes, completion and the learner's own transcript. Visibility (published × audience × role × customer × assignment) is computed once in `_visible_course_names` and reads are then unchecked, because learner roles hold **no DocPerm** on the content doctypes. Every attempt-scoped call asserts `attempt.user == frappe.session.user` by hard equality — no role bypass — before any `ignore_permissions=True` write. Grading is delegated to `training/grading.py` (the only reader of the answer key); checkpoints are served **one at a time** and both served and accepted only when the stored watch intervals cover their timestamp. Gated on Training Settings `training_enabled` + `portal_enabled`: reads then return `{"enabled": false, …}` rather than raising | `get_learner_bootstrap`, `get_course`, `start_attempt`, `get_lesson`, `heartbeat`, `open_checkpoint`, `answer_checkpoint`, `complete_lesson`, `get_quiz`, `submit_quiz`, `finish_attempt`, `get_media_url`, `get_my_transcript` | `/training` player (`public/js/training/player.js`, via a `fetch` transport — no `frappe.*` globals, learners are Website Users with `desk_access = 0`); the Phase-3 builder preview injects its own transport | Google Cloud Storage (signed playback URLs, via `training/gcs_media.py`) |
| `training_ai.py` | Training AI drafting assistant — proposes quiz questions and in-video checkpoints from lesson content, and persists them only once an author accepts. Drafting endpoints **persist nothing** and must never stamp `ai_reviewed_by`: `accept_ai_suggestions` is the human review, and it is what records the reviewer, the model and the grounding quote. `suggest_checkpoints` **refuses without a timed transcript** (lesson or video-asset WebVTT) — without timings a model invents timestamps confidently, and that refusal is the whole integrity story for the feature. Gated on Training Settings `ai_assist_enabled` plus author permission | `draft_quiz_questions`, `suggest_checkpoints`, `accept_ai_suggestions` | Training Canvas page (`training/page/training_canvas`) — the AI drawer, v1.414.0 | Gemini Enterprise Agent Platform (via `gemini.py`) |
| `training_author.py` | Training authoring: draft a new course version, send it for review, publish it, assign and waive, register a Drive video — plus the **authoring backend** (bootstrap, autosave, reorder) shared by the canvas. Publishing is the only Training Manager-gated call — it materializes each lesson's answer-free player payload and its separate permlevel-1 answer key, computes the table of contents and a content hash, `submit()`s the version (which is what freezes it), and applies the author's Minor Edit / Material Change decision to existing completions. `create_draft_version` deep-clones preserving every `lesson_key`/`block_key`/`checkpoint_key`, which is what keeps in-flight learners in place across a typo fix. `get_builder_bootstrap` loads only the **open draft** (`docstatus 0`) and returns `version: null` rather than creating one implicitly; it is the one place in the module that serves `is_correct` to a browser, gated on write permission on the course. `save_draft_version` is autosave: a per-scope field allowlist (`LESSON_/BLOCK_/QUIZ_ROW_/CHAPTER_/VERSION_ALLOWED_FIELDS`), a **mandatory** `modified` optimistic lock, and every non-applied field reported back in `rejected` — see the Gotchas | `create_draft_version`, `submit_for_review`, `publish_version`, `retire_course`, `assign_course`, `waive_assignment`, `register_video_asset`, `get_builder_bootstrap`, `save_draft_version`, `reorder_lessons`; worker `run_bulk_assign` | `public/js/training/training_course.js`; Training Canvas page (`training/page/training_canvas`) | Google Drive (video duration probe) |
| `user_drafts.py` | Per-user form autosave | `save_draft`, `delete_draft`; daily `cleanup_stale_drafts` | `erpnext_enhancements.js` | — |
| `workspace_utils.py` | Workspace shortcut helpers | `add_shortcut_to_workspace`, `get_workspaces_for_user` | `erpnext_enhancements.js` | — |

`telephony.py` whitelisted surface: `get_gateway_config`, `append_call_transcript`, `get_call_transcript`, `get_caller_info`, `update_caller_info`, `log_call_transcript`, `process_unified_recording`, `get_softphone_token`, `get_telephony_routing`, `preview_call_routing`, `receive_mms`, `send_voicemail_email`, `trigger_outbound_call`, `get_employee_number`, `log_call_details`, `process_unified_sms`, `send_sms`.

`get_telephony_routing` is the Triton gateway's whole view of how inbound calls should be handled: the desk softphone identities, the branded caller ID, and (since 1.403.0) a `routing` block carrying the compiled [Call Routing Rules](../ai_governance/README.md). It is one endpoint rather than two on purpose — Triton caches it for 60s and prefetches it while the caller is still listening to the phone menu, so the dial decision never waits on us. `preview_call_routing` is the desk-side counterpart: System Manager only, it answers "who would ring for this call, and why" without placing one.

## Security model

- **`allow_guest=True` (unauthenticated) endpoints:**
  - `logger.log_client_error` — writes an Error Log only; the message is untrusted.
  - `telephony.get_gateway_config` — returns only non-sensitive routing config (no secrets).
  - `telephony.append_call_transcript` / `get_call_transcript` / `get_caller_info` / `update_caller_info` / `process_unified_recording` / `process_unified_sms` and `call_intelligence.process_call_intelligence` — guarded by `@validate_webhook_secret`, which despite its name checks no shared secret: the caller must send a Frappe API key (`Authorization: token key:secret`, what Triton sends), which Frappe verifies before the handler runs, and the request must no longer be Guest. Any enabled API key passes, not only Triton's. Its old `Bearer <admin_webhook_secret>` branch was removed in 1.502.2: Frappe v16 401s an unrecognized Bearer before any handler runs, and the branch compared against the masked `****` placeholder rather than the decrypted secret, so it could never have matched.
  - `telephony.receive_mms` — guarded by `@validate_twilio_request` (HMAC signature).
  - `telephony.get_telephony_routing` — guarded by `@validate_webhook_secret`. Since 1.403.0 its
    `routing` block carries staff **mobile numbers**, resolved from Employee records so the
    gateway needs no lookup of its own. That makes the shared secret on this endpoint load-bearing
    in a way it was not when the payload was two softphone identities: treat a widened response
    here as a disclosure decision, not a plumbing one, and never move it to `get_gateway_config`,
    which is unauthenticated by design.
- **Session-trust:** `time_kiosk` derives the Employee from `frappe.session.user` and rejects a mismatched claimed employee (`_resolve_employee`). The legacy single-point `log_geolocation` still trusts the supplied `employee` for back-compat. `travel.py` uses the same model: `get_my_trips`/`get_itinerary_bootstrap` derive the employee from the session; `get_trip_itinerary`/`get_trip_map_data` gate via `frappe.has_permission`, which the Travel Trip permission hooks scope to owner/crew/coordinators; `send_itinerary_email` lets non-coordinators send only to themselves.
- **Owner-gated, no role bypass:** `training.py` — every attempt-scoped endpoint loads the Training Attempt and refuses unless `attempt.user == frappe.session.user` by hard equality. A Training Manager gets no bypass here on purpose (they use the desk, where the DocPerms and `training/permissions.py` decide); `ignore_permissions=True` is used only *after* that assertion. Learner, employee and customer are always derived from the session — a payload key named `user` or `employee` is dropped. Catalogue visibility is computed in `_visible_course_names` and the content reads that follow are unchecked by design, since learner roles hold no DocPerm on the Training content doctypes at all.
- **Owner-or-reviewer, by hard equality:** `feedback.py` — a request is readable by a `System Manager` or by the person whose `requested_by` equals `frappe.session.user`, and by nobody else. The two intake endpoints are `submit_request` (any signed-in user, for themselves) and `submit_capture` (System Users only, `_require_system_user()`); every other mutating endpoint calls `_require_reviewer()`, i.e. `frappe.only_for("System Manager")` in effect. The DocPerms are the backstop (`System Manager` full, `All` read `if_owner`), and the *absence* of a write grant to the owner is load-bearing rather than an oversight — see the file map row.
- **POST-only:** every one of `training.py`'s thirteen whitelisted endpoints declares `methods=["POST"]` (v1.299.4), and all ten of `feedback.py`'s do for the same reason — a GET puts the request id and the reviewer's decision in the access log, the browser history and the next `Referer`. `tests/test_feedback_endpoint_surface.py` asserts the decorator, that every name in the SPA's `M` map resolves, and that no endpoint is left both un-wired and un-explained. Not CSRF theatre — these carry the attempt id, the lesson key and the checkpoint timestamp, and a GET writes all three into the web server's access log, the browser's history and the `Referer` header of whatever the learner clicks next. The client had always posted and `www/training.html` had always said so in a comment; the server simply never enforced it. `tests/test_training_endpoint_surface.py` asserts the decorator on every endpoint, and separately that every name in the player's `METHOD` map resolves to a real one — a rename with no matching edit there is a 404 the learner sees.
- **A role gate, then the framework's own permissions:** `stock_scan.py` admits `stock_scan_rules.SCAN_ROLES` (System Manager, Stock Manager, Stock User, Inventory Clerk) and then inserts, submits and cancels every Stock Entry and Purchase Receipt as the session user **without** `ignore_permissions` — so User Permissions on Warehouse or Project still apply, and a clerk without Stock User gets a clear refusal at the voucher rather than a page that will not open. Only the `Stock Scan Log` row, the page's own record, is written with `ignore_permissions`. Undo is owner-within-the-window or a Stock Manager (`undo_refusal`); a `client_ref` that belongs to another user's save is refused, not returned. All eleven endpoints are `methods=["POST"]`.
- **Write-permission-gated answer keys:** `training_author.get_builder_bootstrap` is the only call in the Training module that hands `is_correct` (and `correct_text_answers`) to a browser. It gates on `frappe.has_permission("Training Course", "write", doc=course)` on top of `_require_author()` — that gate is the entire difference between an author editing an answer key and a learner reading one, so it must not be relaxed to `read`. The learner side never serves an answer key at all (`training.py`, `training/grading.py`).
- **Role-gated:** `time_kiosk.close_interval_for_employee` -- closing your *own* session needs no role, but naming another employee requires `TIMELINE_MANAGER_ROLES` and is recorded against the requester (`closed_requested_by`). There is deliberately no on-behalf clock-*in* anywhere: a supervisor's phone is not the crew member's, and opening a job is exactly what the geofence exists to prove. `time_kiosk.add_manual_interval` / `update_interval_times` — your own intervals need no role; writing time for **another** employee needs `workforce.approval.APPROVER_ROLES`, via `_assert_may_edit_time_for`. Deliberately not `TIMELINE_MANAGER_ROLES`: that set is pinned to the Location Timeline page's roles, so reusing it would mean letting a supervisor fix a timesheet also hands them everyone's GPS trail — and it is the narrower set, predating the Operations Manager and Production Manager roles the supervisors actually hold. `time_kiosk.get_location_history` / `export_location_history` / `refresh_tracking_health` — only `TIMELINE_MANAGER_ROLES` (`System Manager` / `HR Manager` / `Projects Manager`) may view *another* employee's history; everyone else sees only their own. `get_live_positions` and the full employee picker are manager-only outright. The `get_my_*` views and the correction-request endpoints always resolve the employee from the session. `integrations_health.get_health` / `run_drive_test` — `System Manager` only (`frappe.only_for`), and they return integration secrets only as `configured: true/false`, never the values.
- **Owner-gated:** `comments.update_comment` / `delete_comment` allow edits/deletes only when `comment.owner == frappe.session.user`.
- **Write-permission-gated broadcasts:** `collab.broadcast_field_update` / `broadcast_focus` require **write** permission on the specific document, enforce the settings-driven doctype allowlist (`get_collab_doctypes()` — master switch + child table on ERPNext Enhancements Settings) plus field validation and a value-size cap, and only re-publish to the doc's realtime room (whose membership Frappe's socket.io already permission-checks) — they never persist anything.
- **Project-gated reads (purchasing is *not* separately permission-checked):** `pickup_routing.get_pickup_route_data` gates on `Project.check_permission("read")` and then reads Purchase Orders / Suppliers / Addresses with `frappe.get_all` — i.e. `ignore_permissions=True`. Anyone who can open the job therefore sees its vendors, PO totals and lines regardless of their Purchase Order permissions. Deliberate: it matches the Procurement Tracker on the same Budget tab (`project_enhancements.get_procurement_documents`, whitelisted with **no** gate), and this endpoint is the tighter of the two. Hiding purchasing from some Project readers would mean moving both to `frappe.get_list` together. That call also returns the **Travel Settings Google Maps browser key** to anyone past the gate: a referrer-restricted browser key reaches the client either way (same reasoning as `travel.get_trip_map_data`), and its only real protection is the HTTP-referrer restriction set in the Google Cloud console.
- **`pickup_routing.get_supplier_pick_data` gates on `Purchase Order` read instead, and that is the whole gate.** Deliberately stricter than its project-scoped sibling above, for a structural reason rather than a stylistic one: that endpoint is *anchored* to a Project the caller can already open and shows only that job's spend, whereas this one is anchored to nothing. Without a doctype-level check it would hand any authenticated user every open order, every job and every total for a vendor of their choosing. Every read past the check is `frappe.get_all` (ignore-permissions), so removing it removes all of it — replace it with `frappe.get_list` or not at all.
- **Permission-checked reads:** `activity_log`, `comments` read endpoints, and `search` re-check `frappe.has_permission` / filter with `ignore_permissions=False` before returning data. `gantt.get_gantt_data` treats its whole config as hostile input: `frappe.has_permission` gates the call, rows come from `frappe.get_list` (never `get_all`), every fieldname in the field map / filters / order_by must exist on `frappe.get_meta(doctype)` (start/end restricted to Date/Datetime fieldtypes; uncoercible date values skip the row instead of raising), limits clamp at 1000, and dependency links are dropped unless **both** ends are rows the permission-checked query returned. Composite mode applies the same rules per doctype: the child doctype gets its own `has_permission` gate and validated `get_list`, and child rows are constrained to roots the root query returned. **Writes** (`update_gantt_row`) reuse the same validators: the target doctype must be one the config already addresses, only `start`/`end`/`progress` may change and only via the validated field map, `has_permission(..., doc=name)` is checked per document, submitted docs are refused, and the row is written with `doc.save()` (never `db.set_value`) so validation, doc events and realtime all fire.
- **Role-gated preview:** `telephony.preview_call_routing` is `frappe.only_for("System Manager")`. It answers who would ring for a hypothetical call, which means it discloses the same staff mobile numbers and the shape of the on-call arrangement — so it is gated to the role that can already edit the rules. It also resolves the caller with `create_if_missing=False`, because a *preview* that quietly creates a Customer and Contact for every number somebody types would be its own bug.
- Telephony webhook handlers act as the service user `triton@sapphirefountains.com` and write with `ignore_permissions=True`.

## Gotchas

- `gemini.py` authenticates with an OAuth2 token minted from the Drive service account (`Project Folder Google Drive Settings`), which must hold `roles/aiplatform.user` on the hard-coded GCP project (`sapphire-fountains-poseidon`); `Triton Settings.maps_api_key` is no longer read. The model is a constant (`gemini-3.8-flash`) on the **global** endpoint — 3.1 Pro is served *only* there, and the regional host answered 404 with a message that reads like a missing IAM grant. Google retires the recent Flash models 45 days after a replacement ships, so revisit `MODEL_ID` when a Flash lands; a retired id is a 404 on every call, every caller falls back, and the Integrations Health tile reports the last narrative's source.
- `telephony.get_softphone_token` hard-codes the Twilio identity `"nikolas_erpnext"`.
- `telephony.get_caller_info` has a **write side effect** — it auto-creates a Customer + Contact for unknown numbers and commits, despite reading like a lookup.
- `analytics.py` requires the service-account JSON to be a **Private** file (`/private/files/...`); a public path is rejected to avoid leaking the key. Its docstrings mention raising `ValidationError`, but the code actually returns `{"error": ...}` dicts.
- `maintenance_workflow.check_warranty_and_rma` raises native **Warranty Claims** (one per failed in-warranty water feature); the old Material Request + `"WARRANTY-RETURN-PENDING"` placeholder flow is gone.
- `maintenance_workflow.create_stock_entry` skips consumable rows with qty 0 — dosing sections prefill rows at 0, so untouched chemicals never move stock.
- `workspace_utils.get_workspaces_for_user` returns only **public** workspaces.
- `maintenance_scheduling.update_next_visit_dates` runs once per maintenance submit, from the `on_submit` doc-event in `hooks.py` (the historical duplicate controller call was removed).
- `training_author.save_draft_version` **reports** every field it did not apply, in the response's `rejected` list, instead of dropping it quietly. Autosave that answers "saved" while losing the author's paragraph is worse than a save that fails, and the loss is only discovered on reload. Adding a field to the builder UI therefore means adding it to the matching `*_ALLOWED_FIELDS` set; forgetting surfaces as a warning on the first autosave. Fields the bootstrap emits that are server-owned mirrors (`lesson_key`, `block_key`, `video_duration_seconds`, …) are listed in the `*_ECHOED_KEYS` sets and skipped in silence, because echoing them back is not lost work.
- `save_draft_version`'s `modified` optimistic lock is **mandatory**, unlike `maintenance_visit.save_visit`'s (which tolerates `None`): a caller with no token never loaded the draft. The token is the *version's* `modified` and stands in for the whole builder document — the version is saved unconditionally on every call so the token still advances on a payload that only touched lessons, which are separate documents.
- `save_draft_version` saves the version **before** the lessons. `TrainingLesson._validate_chapter` reads the version's chapter keys from the database, so a lesson moved into a chapter created in the same payload would otherwise be rejected.
- `save_draft_version` manages quiz **pool membership only** (`question`, `points`, `is_required`). A Training Question is a shared, reusable document that can sit in another course's pool, so body fields arriving in a quiz row are refused and reported rather than applied. Question wording is edited on the Training Question form or via `training_ai.accept_ai_suggestions`; checkpoints likewise are not written by autosave.
- `reorder_lessons` writes with `db.set_value(..., update_modified=False)` on purpose: position is not content, and a drag must not be able to fail on an unrelated half-finished block elsewhere in the course. `_draft` has already refused a published version, which is the guard `db.set_value` bypasses.
